var functions = require('./scripts/functions');
//var hello = require('./scripts/functions');
var pixi = require('./scripts/pixi');
var Canvas = require('canvas');

/*test('Tests firing a bullet along a vector facing 90 degrees from default with a velocity of 5.', () => {
    var res = new PIXI.Point(0, 5);
  expect(functions.setAccelInDirection(functions.toRadians(90), 5)).toEqual(res);
});*/